# MinIO LiteLLM Viewer Configuration
# Copy this file to .env and update with your actual values

# MinIO Bucket URL
# Format: http://hostname:port/bucket-name/
# Example: http://minio.example.com:9000/litellm-logs/
BUCKET=http://your-minio-server:9000/your-bucket-name/

# MinIO Access Credentials
# Read-only credentials are recommended for security
ACCESS_KEY=your-minio-access-key
SECRET_KEY=your-minio-secret-key

# Web Server Configuration
# Port for the web application (optional, defaults to 3000)
PORT=3000

# Example configuration for common setups:

# Local MinIO instance:
# BUCKET=http://localhost:9000/litellm/
# ACCESS_KEY=minioadmin
# SECRET_KEY=minioadmin

# OpenMediaVault with MinIO:
# BUCKET=http://openmediavault.local:9000/litellm/
# ACCESS_KEY=your-omv-access-key
# SECRET_KEY=your-omv-secret-key

# Remote MinIO server with HTTPS:
# BUCKET=https://minio.yourcompany.com/logs/
# ACCESS_KEY=readonly-user
# SECRET_KEY=readonly-password

# Notes:
# - The BUCKET URL should include the full path to your bucket with trailing slash
# - Use read-only credentials when possible for security
# - Ensure your MinIO server is accessible from where you run this application
# - The bucket should contain date-organized folders (e.g., 2024-12-25/) with JSON files